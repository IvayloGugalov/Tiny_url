import '@mui/material/styles'

// This file is kept for future custom color extensions.
//
// Standard colors used in the project:
// - primary (main, light, dark)
// - secondary (main, light, dark)
// - info (main, light, dark)
// - success (main, light)
// - error (main, light)
// - warning (main)
// - background (default, paper)
// - text (primary, secondary)
// - common (white)
// - divider

declare module '@mui/material/styles' {
  // Uncomment and extend these interfaces when adding custom colors
  // interface Palette {
  //   customColor: PaletteColor
  // }
  // interface PaletteOptions {
  //   customColor?: PaletteColor
  // }
  // interface PaletteColor {
  //   darker?: string
  //   lighter?: string
  // }
}
